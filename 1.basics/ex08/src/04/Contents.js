import React from "react";

function Contents(props) {
    // return <p>Pure React(React API)로 컴포넌트</p>;

    return React.createElement("p", null, "Pure React(React API)로 컴포넌트");
}

export default Contents;
